# Go build outputs
/out/
*.test
*.out

# Svelte / Vite build outputs (regenerated by Dockerfile at build time;
# committing the empty placeholder so go:embed has something to point at).
web/dist/
!web/dist/.gitkeep

# Node deps
web/node_modules/

# Editor / OS
.DS_Store
.vscode/
.idea/
