{
  "name": "tor-dice-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Svelte SPA for the TOR dice roller (homelab#262). Built as part of the tor-dice Docker image multi-stage build; ships as static files embedded into the Go server binary via //go:embed.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "svelte": "^5.55.0",
    "vite": "^8.0.0"
  }
}
